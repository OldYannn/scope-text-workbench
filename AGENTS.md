# AGENTS.md

## 项目使命

本仓库用于开发一套面向人文社会科学研究者的：

- 开源；
- 本地优先；
- AI 原生；
- 可复现；

文本分析工作台。

在进行任何架构级、产品级或研究方法级决策前，必须先完整阅读：

`PROJECT_BRIEF.md`

---

## 品牌和命名规则

- 项目正式名称暂定为 `SCOPE`，中文品牌名为“文镜”；
- 中文界面、中文宣传和国内用户场景中可以使用“SCOPE 文镜”；
- 国际 GitHub、软件论文和英文文档中以“SCOPE”为主要品牌；
- 代码变量、类、函数、接口和数据库字段继续使用规范英文命名；
- GitHub 仓库名、Package Name、Bundle ID、应用内部标识等技术名称未经项目负责人确认不得锁定；
- `SCOPE Framework` 目前只是长期方法学框架工作名，不得擅自解释五个字母，或据此发明方法、指数、权重、阈值和学术结论。

---

## 项目语言规则

- 源代码、变量名、函数名、类名、API / Protocol 字段、JSON Schema、Package Name、配置字段、目录技术名称和 Git Commit 使用规范英文；
- 面向项目负责人和研究者的人类文档以中文为主，关键英文技术术语可保留，并应在首次出现时用中文解释；
- 文件名不要求中文化，`development.md`、`sidecar-protocol.md` 等英文文件名可以保留；
- `PROJECT_BRIEF.md`、`AGENTS.md`、`ROADMAP.md`、`docs/` 下的设计与方法文档，以及用户文档，必须让非计算机专业的项目负责人能够直接理解；
- 每项重要架构决策都应包含简短的“项目负责人说明”，用非专业中文解释设计原因、解决的问题、主要风险和需要项目负责人决策的事项；
- Private / Pre-alpha 阶段的 `README.md` 以中文为主；进入 Public Alpha 前，再规划英文主版 `README.md` 和中文版 `README.zh-CN.md`；
- 不得为了表面统一而翻译协议字段、命令、路径、包名或其他需要保持技术一致性的标识。

---

## 开发优先级

优先级从高到低：

1. 研究方法正确性；
2. 可复现性；
3. 用户数据隐私；
4. 可维护性；
5. 自动化测试；
6. 非编程研究者的易用性；
7. 性能；
8. 功能数量。

不得为了快速 Demo 而牺牲研究方法正确性或可复现性。

---

## 范围控制

当前阶段不试图替代：

- NVivo；
- MAXQDA；
- Gephi；
- CiteSpace；
- Zotero；
- 完整统计软件。

不要主动增加与 MVP 无关的功能。

MVP 范围严格按照 `PROJECT_BRIEF.md` 中的 Milestone 推进。

---

## 开发规则

- GUI 测试、Computer Use 和项目负责人人工验收遵循 `docs/testing/gui-testing.md`；默认自动化验证优先，Computer Use 只作最小必要补充；
- 优先进行小范围、可审查的修改；
- 保持模块边界清晰；
- 避免过早进行复杂抽象；
- 外部模型 Provider 必须有统一接口；
- 研究算法与 UI 必须解耦；
- 本地分析引擎与在线 AI Provider 必须解耦；
- 修改算法行为时必须增加或更新回归测试；
- 使用固定 Fixtures 验证算法；
- 不得静默修改影响研究结果的默认参数；
- 任何可能改变研究结论的默认值都必须写入文档；
- 依赖版本应合理固定或限定；
- 除非打包流程需要，不要提交大量自动生成构建文件。

---

## AI / LLM 开发规则

- LLM 必须保持可选；
- 不得将项目写死在单一 Provider 上；
- 不得提交 API Key、Token、密码或其他 Secret；
- 不得将 API Key 写入日志；
- 将用户语料发送到外部服务前必须明确提示；
- 科研编码功能优先使用 Structured Output；
- 结构化输出必须进行 Schema Validation；
- 尽可能保存可复现 AI 分析所需的模型和参数信息；
- 将模型输出视为可能出错的数据，不得视为 Ground Truth。

---

## 研究算法规则

每新增一个研究分析算法前：

1. 先定义方法；
2. 明确输入；
3. 明确输出；
4. 明确关键参数；
5. 明确默认值；
6. 准备固定测试数据；
7. 编写测试；
8. 再完成 UI。

对于涉及随机性的算法：

- 尽可能提供 Random Seed；
- 在分析日志中记录 Seed。

不得自行发明：

- 学术阈值；
- 指数权重；
- 可靠性分级；
- 学术结论。

除非项目负责人明确批准，并且存在方法学依据。

---

## 文档规则

用户可见行为发生变化时，应同步更新对应文档。

研究方法发生变化时，应更新方法说明。

适当更新：

- README.md；
- CHANGELOG.md；
- ROADMAP.md；
- `/docs`；
- Tutorial；
- 示例。

`README.md` 主要面向 GitHub 用户。

`PROJECT_BRIEF.md` 保存长期产品、技术和研究战略。

人类文档的语言和架构说明要求遵循“项目语言规则”。

---

## Git 规则

- 优先使用聚焦单一目的的 Commit；
- 非显而易见的架构决策需要说明原因；
- 未经明确授权不得使用破坏性 Git 操作；
- 未经明确授权不得 Rewrite History；
- 宣布 Milestone 完成前必须运行相关：
  - Build；
  - Test；
  - Lint；
  - Format；
- 工作完成时说明：
  - 修改了哪些文件；
  - 完成了什么；
  - 执行了哪些验证；
  - 仍有哪些已知问题。

---

## 必须征求项目负责人同意的事项

以下事项不得自行决定：

- 项目品牌、正式名称及对外命名变更；
- 软件 License；
- 更换核心应用框架；
- 将核心功能依赖 SaaS；
- 新增会发送用户本地语料的第三方服务；
- 破坏性改变项目文件格式；
- 删除已支持功能；
- 重大架构迁移；
- 宣布某个版本可以公开 Release。

普通、低风险且符合既定架构的实现细节无需反复询问。

---

## Definition of Done

一个功能不能因为“界面能点了”就算完成。

在适用情况下，至少满足：

- 功能代码完成；
- 测试通过；
- 算法结果可验证；
- 参数有记录；
- 错误状态已处理；
- 结果能够复现；
- 没有暴露 Secret；
- 用户文档同步；
- 研究方法说明同步。

---

## Codex 第一次进入仓库时

如果项目仍处于初始或 Pre-alpha 状态：

1. 阅读 `PROJECT_BRIEF.md`；
2. 检查当前仓库；
3. 不要立即进入大规模开发；
4. 提供架构方案比较；
5. 给出 Milestone 0 执行计划；
6. 指出假设和风险；
7. 推荐最小但技术上可信的 Public Alpha 基础架构；
8. 等待项目负责人确认后再进入主体开发。
