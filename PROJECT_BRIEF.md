# 项目总纲 — SCOPE 文镜

> 项目状态：Pre-alpha / 项目立项阶段
> 最后更新：2026-08-10
> 项目正式名称（暂定）：SCOPE
> 中文品牌名：文镜
> 中文对外名称：SCOPE 文镜

---

## 1. 项目愿景

开发 SCOPE，一套面向人文社会科学研究者的、开源、本地优先、可复现、AI 原生的文本分析工作台。中文品牌名为“文镜”，中文界面、中文宣传和国内用户场景中可使用“SCOPE 文镜”。

本项目受到 ROST CM6 等低门槛中文文本分析工具的启发，但**不是对 ROST CM6 的直接复刻**。

项目希望保留传统图形化文本分析软件“无需编程即可使用”的优势，同时整合：

- 现代中文自然语言处理能力；
- 可复现研究机制；
- 文本网络分析；
- 可选的大语言模型（LLM）分析能力；
- 人机协同编码；
- 研究过程审计和结果导出。

项目的核心产品主张是：

> 让不具备编程背景的人文社会科学研究者，也能够完成从原始文本到透明、可复现、可导出的文本分析流程。

---

## 2. 目标用户

主要面向使用文本资料开展研究的人文社会科学研究者、教师和学生，包括但不限于：

- 公共管理与政策研究；
- 社会学与社会工作；
- 新闻传播学；
- 教育学；
- 图书情报与信息资源管理；
- 旅游与管理学；
- 政治学；
- 其他需要进行文本分析的人文社会科学领域。

---

## 3. 核心原则

### 3.1 研究方法优先，而不是功能堆叠

任何新增分析功能都必须能够对应到明确的研究方法、研究问题或分析流程。

不能仅仅因为某个 NLP 或 AI 库“容易调用”，就将其加入软件。

每个主要分析模块都至少需要回答：

1. 这个功能解决什么研究问题？
2. 方法基于什么假设？
3. 哪些参数会影响研究结果？
4. 结果能否复现？
5. 研究者应如何在论文中报告这一分析过程？

---

### 3.2 本地优先（Local-first）

用户的语料应默认保存在本地。

研究者可能处理：

- 访谈资料；
- 未公开论文；
- 内部政策文件；
- 问卷开放题；
- 医务、社会工作材料；
- 组织内部文件；
- 尚未发表的研究数据。

除非用户主动调用在线大模型/API 功能，否则不得将语料发送到外部服务。

---

### 3.3 LLM 是增强能力，不是软件基础

即使用户：

- 没有配置 API Key；
- 当前处于离线状态；
- 不希望将任何数据发送给外部模型；

软件仍然应该能够完成传统文本分析工作。

因此，LLM 功能必须作为独立增强层存在，而不能成为核心功能运行的必要条件。

---

### 3.4 AI Provider 必须保持中立

禁止将软件架构写死在 DeepSeek 或其他单一模型服务商上。

应设计统一的 Provider Adapter 层，未来至少能够兼容：

- DeepSeek；
- OpenAI；
- Gemini；
- Claude；
- OpenRouter；
- Ollama / 本地模型；
- 通用 OpenAI-compatible API。

用户可配置字段可包括：

- API Base URL；
- API Key；
- 模型名称；
- Temperature；
- 最大输出 Token；
- Structured Output / JSON Schema 等。

API Key 不得提交到 Git 仓库中。

---

### 3.5 默认保障可复现性

每一次分析都应尽可能保存足够的元数据，使研究者以后能够重新运行并复现结果。

建议保存：

- 软件版本；
- 数据集标识与哈希；
- 分析时间；
- 分析方法；
- 方法参数；
- 停用词表版本；
- 用户词典版本；
- 随机种子；
- 模型与 Provider；
- Prompt 模板；
- 模型参数；
- 失败记录；
- 人工修订记录；
- 输出 Schema。

---

### 3.6 Vibe Coding 可以加速工程，但不能替代验证

允许大量使用 Codex 等 AI 编程工具提高：

- UI 开发；
- 工程实现；
- 文件处理；
- API 接入；
- 重构；
- 文档编写；
- 自动化测试编写效率。

但是研究算法的正确性必须通过：

- 明确的方法定义；
- 固定测试数据；
- 单元测试；
- 回归测试；
- 人工抽查；
- 必要时与已知结果对比；

进行验证。

不能以“看起来结果合理”作为科研算法正确性的依据。

---

## 4. 本项目当前不准备做什么

第一阶段不试图同时成为：

- NVivo 替代品；
- MAXQDA 替代品；
- Gephi 替代品；
- CiteSpace 替代品；
- Zotero 替代品；
- 完整统计软件；
- 通用 ChatGPT 客户端；
- AI 学术写作软件。

必须严格控制功能范围，避免项目失控。

---

## 5. 第一阶段总体目标

第一阶段的目标是：

> 建立一个技术基础可靠、公开可访问的开源 GitHub 项目，并最终发布一个能够跑通基础文本分析流程的 v0.1 Public Alpha。

第一阶段优先级：

1. 架构正确；
2. 算法正确；
3. 测试完整；
4. 数据隐私；
5. 可复现；
6. 文档清晰；
7. 功能数量。

第一阶段成功标准：

外部研究者可以：

1. 安装并运行软件；
2. 导入一个小规模文本数据集；
3. 完成基础文本清洗；
4. 完成中文分词；
5. 完成词频或共词分析；
6. 查看分析参数；
7. 导出数据和图表；
8. 重复执行分析并得到一致结果；
9. 可选配置一个 LLM Provider；
10. 完成一个基础的结构化 AI 编码任务。

---

## 6. MVP 功能范围

### 6.1 语料库管理

第一阶段支持：

- TXT；
- CSV；
- XLSX；
- DOCX。

功能：

- 创建项目；
- 打开项目；
- 导入语料；
- 数据预览；
- 选择文本字段；
- 保留元数据字段；
- 删除重复记录；
- 基础语料统计。

PDF 暂缓，除非能够确定稳定可靠的解析方案。

---

### 6.2 文本清洗

支持可配置的基础清洗操作，例如：

- 空白字符规范化；
- URL 删除；
- HTML 标签清理；
- 标点处理；
- 正则替换；
- 重复文本删除；
- 停用词表；
- 自定义停用词。

所有清洗操作都应记录进项目分析历史。

---

### 6.3 中文分词

第一阶段可采用成熟的中文分词库，例如 jieba，但应通过抽象接口封装。

功能：

- 标准分词；
- 自定义词典；
- 停用词；
- 可选词性信息；
- 分词结果预览。

不要让底层第三方库的实现细节成为永久性的产品概念。

---

### 6.4 基础文本统计

MVP 包括：

- 词频；
- 文档频率；
- N-Gram；
- TF-IDF；
- 关键词排序。

支持导出：

- CSV；
- XLSX。

---

### 6.5 共现分析

MVP 支持：

- 设置共现单位；
- 文档级共现；
- 条件允许时支持段落或滑动窗口；
- 生成共现表；
- 生成共现矩阵；
- 最低词频阈值；
- 边权阈值；
- 基础网络预览。

支持导出：

- CSV；
- GraphML。

第一阶段不需要复刻 Gephi 的全部网络可视化能力。

正确生成网络数据并方便导入 Gephi，比复杂的内置网络可视化更重要。

---

### 6.6 基础可视化

第一阶段可以提供：

- 高频词柱状图；
- 词频分布；
- 基础共现网络预览。

图表应尽可能支持适合论文使用的格式导出。

---

### 6.7 LLM 结构化编码（实验性）

第一阶段 AI 功能不要做成通用聊天机器人。

应聚焦一个明确的科研能力：

> 文本结构化分类 / 编码。

用户可定义：

- 编码维度；
- 分类类别；
- 类别定义；
- Prompt；
- Few-shot 示例；
- 期望 JSON Schema。

模型应优先返回结构化结果，而不是自由文本。

系统需要保存：

- Provider；
- 模型名称；
- Prompt；
- 模型参数；
- 输入记录 ID；
- 原始响应（在合适情况下）；
- 解析后的结构化结果；
- 错误和重试；
- Token / 成本信息（如果 API 提供）。

早期版本必须明确标记：

> Experimental / 实验性功能。

---

## 7. 架构方向

Milestone 0 主架构确定为：

> Tauri 2 + React / TypeScript + 薄 Rust 桌面壳 + 打包后的 Python 分析引擎 sidecar。

Electron + React / TypeScript + Python sidecar 作为跨平台打包验证失败时的回退方案。本地 FastAPI + pywebview 保留为快速原型或备选方向，不作为当前主架构。

该决策不因项目更名为 SCOPE 而改变。

### 7.1 桌面 / 本地应用优先

- React / TypeScript 负责用户界面；
- Rust 保持为薄桌面协调层，负责窗口、权限、获批路径、应用更新和 sidecar 生命周期；
- Python 分析引擎负责项目存储、SQLite、研究算法、参数默认值、结果持久化和研究审计链；
- UI 与 Python sidecar 通过版本化的本地协议通信，大语料和结果只传项目内引用与哈希；
- 本地分析路径默认离线，LLM Provider 作为后续独立且可选的外部 Adapter。

Milestone 0 必须通过真实安装产物持续验证：

- Windows/macOS 打包复杂度；
- Python 集成难度；
- 内存占用；
- 安装包体积；
- 自动更新；
- 开发效率；
- 长期维护成本；
- Vibe Coding 友好程度；
- 安全性。

目标平台：

- Windows；
- macOS。

Linux 支持是加分项，但不要求第一版完成。

---

### 7.2 分析引擎

可能使用的 Python 生态包括：

- pandas；
- numpy；
- jieba 或其他中文分词器；
- scikit-learn；
- networkx；
- matplotlib / Plotly；
- sentence-transformers（后期）；
- BERTopic（后期）。

不能为了“以后可能用”提前引入大量依赖。

---

### 7.3 项目数据存储

优先设计可迁移的项目文件结构。

可考虑：

- SQLite：保存项目元数据、分析记录；
- 项目目录：保存语料和导出结果；
- JSON/YAML：保存分析配置。

长期目标：

> 一个项目可以完整复制到另一台电脑继续工作。

---

## 8. 研究审计链（Research Audit Trail）

这是本项目未来最重要的差异化能力之一。

架构从第一阶段就必须兼容。

每次分析应保存类似：

```json
{
  "software_version": "0.1.0-alpha",
  "analysis_id": "uuid",
  "dataset_hash": "...",
  "method": "cooccurrence",
  "parameters": {},
  "resources": {
    "stopwords": "...",
    "dictionary": "..."
  },
  "created_at": "...",
  "random_seed": 42
}
```

如果是 LLM 分析，应额外保存：

```json
{
  "provider": "deepseek",
  "model": "...",
  "prompt_template": "...",
  "temperature": 0,
  "output_schema": {},
  "success_count": 0,
  "failure_count": 0
}
```

长期目标：

> 一键导出“研究方法记录”，方便研究复现、论文方法部分撰写和补充材料归档。

---

## 9. 人机协同可靠性分析：未来研究方向

这不是 MVP 的核心开发任务，但早期数据结构应为这些功能留出空间。

未来可支持：

- 人工编码 vs AI 编码；
- AI 模型 vs AI 模型；
- 重复运行稳定性；
- Cohen's Kappa；
- Krippendorff's Alpha；
- Accuracy；
- Precision；
- Recall；
- F1；
- 人工复核抽样；
- 低置信度复核队列；
- Prompt 敏感性分析。

这一部分未来可能发展成为软件关联的主要方法学研究方向。

---

## 10. 学术发展路线

论文不是当前第一阶段的执行重点，但软件必须保留未来开展学术研究所需的数据和证据。

### 10.1 SCOPE Framework（长期方法学工作名）

> SCOPE Framework 是本项目未来计划发展的、面向人文社会科学文本研究的可复现 Human-AI Content Analysis 方法框架工作名。

它目前不是已经建立或验证完成的方法论。现阶段不正式定义 S、C、O、P、E 五个字母分别代表什么，也不得为了凑缩写发明方法、指数、权重或可靠性阈值。

未来应在 Research Audit Trail、AI Coding、Human-AI Reliability、Prompt / Model Reproducibility 等能力形成并经过实际验证后，再从真实方法流程反向确定 SCOPE Framework 的严格学术定义。

长期希望以 SCOPE Software 承载语料管理、文本挖掘、共现与网络、AI 编码、研究审计链和可靠性分析，并在此基础上逐步发展和验证 SCOPE Framework。软件品牌与方法框架相关联，但不得把尚未验证的方法框架宣传为现有软件能力。

围绕软件、方法框架和实际研究应用，长期可形成三个论文方向。

### 10.2 软件论文

目的：

- 为 SCOPE Software 建立正式可引用的学术身份；
- 介绍 SCOPE 的软件需求、架构、设计、可复现机制和研究用途。

未来可考虑：

- Journal of Open Source Software（JOSS）；
- 信息资源管理 / 科研软件相关期刊；
- 其他合适的软件或方法类期刊。

---

### 10.3 方法论文

未来最重要的方法方向之一：

> 在形成充分的软件实践和方法学证据后，正式提出并验证 SCOPE Framework。

潜在研究问题包括：

- Human-AI 编码可靠性；
- 多模型比较；
- Prompt 敏感性；
- LLM 输出稳定性；
- Human-in-the-loop；
- 成本与可靠性的权衡；
- AI 时代社会科学内容分析的可复现规范。

方法论文未来可能成为比软件论文本身更重要的引用入口。

---

### 10.4 应用论文

可利用 SCOPE Software，并在方法框架成熟后使用 SCOPE Framework，开展：

- 政策文本分析；
- 新闻框架分析；
- 社交媒体研究；
- 访谈文本分析；
- 公共管理研究；
- 社会工作研究。

重要原则：

> 不得为了发表论文而拍脑袋创造指数、权重或可靠性阈值。

任何新方法必须经过严谨验证。

---

## 11. 开源策略

项目不应等到 1.0 正式版才第一次公开。

建议在 Milestone 0 基础完成后即可公开 GitHub 仓库，并明确标记：

> Pre-alpha / Under active development

初始公开仓库至少包括：

- README.md；
- LICENSE；
- AGENTS.md；
- PROJECT_BRIEF.md；
- ROADMAP.md；
- CONTRIBUTING.md；
- CHANGELOG.md；
- SECURITY.md；
- CITATION.cff；
- Issue Templates；
- 源代码；
- 自动化测试；
- CI 配置。

License 建议后续比较：

- Apache-2.0；
- MIT；
- GPL-3.0。

未经项目负责人明确同意，Codex 不得自行决定 License。

不要自创许可证强制研究者引用论文。

未来应通过以下方式鼓励规范引用：

- README Citation；
- CITATION.cff；
- GitHub Release；
- Zenodo DOI；
- 研究结果导出中的 citation metadata；
- 软件论文。

---

## 12. 仓库质量要求

任何 Milestone 宣布完成前至少检查：

- 主分支能够正常构建；
- 自动化测试通过；
- Lint / Format 检查通过；
- 不存在 API Key 或敏感信息；
- 用户可见变更已有文档；
- 改动研究结果的功能必须有回归测试；
- Breaking Change 有记录；
- 导出结果经过人工抽查；
- 核心算法具有固定测试样本。

优先使用小规模、可审查的 Commit。

不要无理由进行全仓库大规模重写。

---

## 13. 开发里程碑

### Milestone 0 — 仓库基础

目标：

> 建立长期可维护的开源项目骨架。

交付：

- 仓库结构；
- 技术架构决策；
- README；
- AGENTS.md；
- PROJECT_BRIEF.md；
- ROADMAP；
- License 方案比较；
- CI；
- 测试框架；
- 最小可运行应用壳。

---

### Milestone 1 — 语料管理 + 文本清洗

交付：

- 创建/打开项目；
- TXT/CSV/XLSX/DOCX 导入；
- 数据预览；
- 文本字段选择；
- 基础清洗 Pipeline；
- 项目持久化；
- 自动化测试。

---

### Milestone 2 — 分词 + 高频词分析

交付：

- 中文分词；
- 停用词管理；
- 用户词典；
- 词频；
- TF-IDF；
- N-Gram；
- 数据导出；
- 固定测试数据验证。

---

### Milestone 3 — 共现分析 + 可视化

交付：

- 共现矩阵；
- 网络生成；
- 阈值配置；
- GraphML；
- 基础网络可视化；
- 回归测试。

---

### Milestone 4 — 研究审计链

交付：

- Analysis Manifest；
- Dataset Hash；
- 方法与参数历史；
- 重复运行；
- 研究方法记录导出。

---

### Milestone 5 — LLM Provider Layer

交付：

- Provider 抽象；
- 安全的 API Key 配置；
- OpenAI-compatible 接口；
- 至少一个 Provider 实现；
- Structured Output；
- 错误处理；
- 重试机制。

---

### Milestone 6 — AI 编码实验模块

交付：

- 编码框架编辑器；
- Prompt 模板；
- Few-shot；
- 批量结构化编码；
- JSON Schema 验证；
- 结果审核；
- 人工修订；
- AI Audit Log。

---

### Milestone 7 — v0.1 Public Alpha

发布条件：

- 完整基础工作流可运行；
- 外部测试者能够按照文档完成安装；
- 已明确已知限制；
- 核心算法具有自动化测试；
- 至少提供一个示例数据集或 Tutorial；
- GitHub Issues / Discussions 已准备；
- 不存在已知严重数据安全问题。

---

## 14. 产品 UX 方向

产品应表现为：

> 现代化研究工作台

而不是：

> 开发者工具 / 聊天机器人。

优先：

- 清晰的项目工作流；
- 第一阶段中文优先；
- 使用明确的研究方法术语；
- 参数透明；
- 支持重新运行；
- 显示可复现信息；
- 导出清晰；
- 错误提示友好。

避免：

- Chatbot-first；
- 用“AI 魔法”隐藏研究参数；
- 信息密度过高；
- 为视觉效果而增加复杂操作。

长期导航可以考虑：

1. 项目 / 语料；
2. 数据预处理；
3. 文本统计；
4. 共词 / 网络；
5. AI 编码；
6. 可靠性；
7. 可视化；
8. 研究记录；
9. 导出。

---

## 15. 研究向导：长期差异化

暂不属于 MVP。

未来可以提供面向研究问题的 Wizard。

例如：

### 政策文本分析向导

1. 定义研究问题；
2. 导入政策文本；
3. 文本切分；
4. 建立编码框架；
5. 预测试；
6. 人工验证；
7. 正式编码；
8. 一致性分析；
9. 统计与可视化；
10. 导出研究方法记录。

长期目标：

> 软件不仅告诉研究者“点哪个按钮”，还帮助其理解“为什么应该使用这个方法”。

---

## 16. 数据安全和隐私

最低要求：

- API Key 不进入源码；
- API Key 不写入日志；
- 调用外部 API 前明确提示；
- 只发送当前任务真正需要的文本；
- UI 中明确区分本地分析与在线分析；
- 合理处理导入文件名、路径和文本内容；
- CI 中适当加入依赖漏洞检测；
- 不采集不必要的用户研究数据。

---

## 17. 项目命名

项目正式名称暂定为 **SCOPE**，中文品牌名为 **文镜**。

- SCOPE 是软件和开源项目的核心品牌名称；
- 中文界面、中文宣传和国内用户场景中可以使用“SCOPE 文镜”；
- 国际 GitHub、软件论文和英文文档中以“SCOPE”为主要品牌；
- 代码中的变量名、类名、函数名、接口和数据库字段继续采用规范英文命名；
- “SCOPE Framework”是长期方法学框架工作名，不代表该方法已经建立或验证完成。

品牌名称已经确定，GitHub 仓库名称也已确认为 `OldYannn/scope-text-workbench`。其余技术标识尚未锁定；候选名称均未完成商标或生态冲突核验，必须由项目负责人确认后实施：

| 技术标识 | 候选方向（未锁定） | 主要风险 |
|---|---|---|
| GitHub Repository Name | 已确认：`OldYannn/scope-text-workbench` | 仓库链接已经形成，不再作为普通实现细节变更 |
| 应用显示名 | 国际场景 `SCOPE`；中文场景 `SCOPE 文镜` | 显示名可含空格和中文，但不能直接当作全部技术标识 |
| 应用内部标识 | `scope-desktop`、`scope-workbench` | 需要与 Bundle ID、更新通道和可执行文件名保持可追踪关系 |
| Tauri / Bundle ID | `org.<maintainer>.scope`、`io.github.<owner>.scope` | 必须先确定权威域名或 GitHub Owner；发布后不宜更改 |
| JavaScript Package Name | `@<owner>/scope-desktop`、`@scope-wenjing/desktop` | npm scope 和包名可用性尚未核验 |
| Python Distribution Name | `scope-research-engine`、`scope-text-engine` | PyPI 名称可能冲突；distribution name 与 import name 可以不同 |
| Python Import Package | `scope_engine`、`scope_research_engine` | 应避免使用过于通用的 `scope`，降低与其他库冲突的风险 |
| Sidecar / CLI 名称 | `scope-engine`、`scope-cli` | 可能与系统命令或其他工具冲突；MVP 不应无必要暴露全局 CLI |
| 项目格式或扩展名 | 项目目录、`.scope-project` 等候选 | 一旦公开使用会形成兼容性承诺，必须先设计迁移和冲突处理 |

在上述标识确认前，可以使用明确标注为开发用途、可替换且不进入公开 Release 的内部占位符。不要注册正式域名、发布正式 Package，或创建难以迁移的公开标识。

### 17.1 项目语言规范

项目采用“技术标识保持英文、人类文档中文优先”的长期语言规范：

- 源代码、变量和函数、API / Protocol 字段、JSON Schema、Package Name、配置字段、目录技术名称与 Git Commit 保持规范英文；
- 面向项目负责人、研究者和中文用户的人类文档以中文为主，首次出现的关键英文技术术语应提供简短中文解释；
- 文档文件名无需强行中文化，但正文必须让非计算机专业的项目负责人能够直接理解；
- 每项重要架构决策应提供“项目负责人说明”，解释设计原因、解决的问题、主要风险和待决策事项；
- Private / Pre-alpha 阶段的 `README.md` 以中文为主，进入 Public Alpha 前再规划英文主版 `README.md` 与中文版 `README.zh-CN.md`。

语言规范用于降低项目维护和研究沟通门槛，不要求对源代码和技术标识进行无意义翻译。

---

## 18. 当前阶段与下一步

架构比较、推荐方案、目录结构、Milestone 0 执行计划和主要风险分析已经完成。当前继续执行 Milestone 0，不重新进行架构选型，也不提前进入 Milestone 1 的语料管理和文本清洗开发。

下一步依次为：

1. GitHub Repository Name 已确认；继续由项目负责人确认第 17 节中的 Bundle ID、Python / JavaScript Package Name 等发布标识；
2. Architecture Decision Record、模块职责、Python sidecar 初始协议和 Electron 回退条件已经建立；
3. 最小仓库骨架、测试框架和 CI 已经建立；
4. 非科研性质的 diagnostic tracer bullet 已完成，已验证进度、取消、异常中断和可复现清单；
5. Python sidecar 已在 Windows x64、macOS arm64、macOS x64 上完成原生冻结和 CI 打包，Release 不依赖用户系统 Python；
6. 下一步在三个目标平台验证安装、首次启动和最小 GUI smoke test，并单独进行项目负责人 UAT；
7. 完成 Milestone 0 所需的开源项目基础文档和质量检查。

以下事项最终决策权属于项目负责人：

- 项目品牌、正式名称及对外命名变更；
- License；
- 核心框架；
- 数据隐私政策；
- 重大功能范围调整；
- 公开版本发布；
- 重大架构迁移。
